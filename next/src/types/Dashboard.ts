export interface CustomerBalance {
    // Define the properties for CustomerBalance
    customer_name: string;
    balance: number;
}

export interface SupplierBalance {
    // Define the properties for SupplierBalance
    supplier_name: string;
    balance: number;
}

export interface StockBalance {
    // Define the properties for StockBalance
    item_name: string;
    balance_qty: number;
}